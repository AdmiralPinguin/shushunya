# Temporary Mechanicum Holding

This folder holds large or parked Mechanicum-adjacent subsystems while the
active worker/governor layout is being reorganized.

- `AgentArena/` is parked here until agent competition and benchmark work
  resumes.
- `RoxDub/` is parked here until the dubbing/mobile media workflow is reviewed
  separately.

Do not route active EyeOfTerror worker pipelines through this folder unless a
subsystem is explicitly restored.
