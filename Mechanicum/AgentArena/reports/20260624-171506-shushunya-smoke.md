# Shushunya Smoke After SWE Loop Changes

Run ID: `20260624-171506`

Agent revision: `f44a0dc`

## Results

| Task | Result | Duration | Notes |
| --- | ---: | ---: | --- |
| `artifact_contract` | pass | 219.409s | Created and verified the required report in sandbox `/work`. |
| `repair_loop` | pass | 428.159s | Used a clean reproduce-read-edit-verify flow. |

## Repair Loop Trace

The repair task completed in five agent actions:

1. `shell ls -la` in the task workspace.
2. `shell python3 -c ...` to reproduce the failing assertion.
3. `read_file calc.py`.
4. `replace_in_file` changing subtraction to addition.
5. same `shell python3 -c ...` verification, now passing.

This confirms the supervisor fix that allows repeating the same verification
after a state-changing edit. It also confirms the shell-failure guidance steered
the model away from repeating the same failing diagnostic.

## Comparison To Baseline

Baseline `20260624-151112`:

- `artifact_contract`: 246.806s
- `repair_loop`: 587.129s

After `b3d5dc3` and `f44a0dc`:

- `artifact_contract`: 219.409s
- `repair_loop`: 428.159s

The main quality improvement is not only time; the repair loop became more
direct and closer to mini-SWE's core workflow: reproduce, inspect relevant file,
edit, verify.

